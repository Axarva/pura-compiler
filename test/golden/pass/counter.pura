initialModel : Int
let initialModel = 0

update : String -> Int -> Int
-- let update = msg => model =>
--  if msg == 0 then (model + 1) else (if msg == 1 then (model - 1) else model)
let update = msg => model => {
  if msg == "0" then {
   model + 1
  } else {
    if msg == "1" then {
      model - 1
    } else model
   }
}

view : Int -> Html String
let view = model => {
  div [onClick "2"] [
    h1 [onClick "2"] [text "Pura Counter"],
    p [onClick "2"] [text ("Count: " ++ (toString model))],
    button [onClick "0"] [text "+"],
    button [onClick "1"] [text "-"]
  ]
}
